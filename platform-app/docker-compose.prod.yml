version: "3.8"

services:
  app:
    image: cr.yandex/${CR_ID}/acp-platform:latest
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    environment:
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
      - PORT=3000
